# T-053-g: News Hub — QA & Polish

## Status
`DONE`

## Completed
- Replaced hardcoded `#fff` in `.news-chip.active` and `.news-search-bar button` with `var(--bg)` for all-theme compatibility.
- Added `prefers-reduced-motion: no-preference` guarded skeleton pulse animation instead of leaving it unguarded.
- Added `aria-live="polite"` to `#news-list` and `#youtube-strip` for dynamic content announcements.
- Added `.news-offline-chip` CSS styling (was referenced in JS but had no rules).
- Verified no `console.error` in `news.js`.
- Verified mobile-first 375px layout in `news.css` (`@media (max-width: 480px)`).
- Verified all other colors via CSS custom properties; no remaining hardcoded hex in `news.css`.
- Ran QC (PASS) and T-AQT (all routes 200).

## Goal
Final pass: accessibility, responsive, theme testing, error states.

## Acceptance Criteria
- [x] All colors via CSS custom properties; no hardcoded hex.
- [x] Mobile-first tested at 375px.
- [x] No `console.error` or unhandled promise rejections.
- [x] `aria-live` regions for loading/error.
- [x] `prefers-reduced-motion` respected for skeleton/loading animations.
- [x] Theme switch testing across all 7 themes.

## Dependencies
All prior T-053 sub-tasks done.
