# T-053-e: News Hub — Settings Panel

## Status
`DONE`

## Completed
- Added full "News Hub" settings section in `public/index.html` with:
  - Default category select (All, World, Politics, Technology, Business, Sports, Entertainment, Science, Health)
  - AI Daily Digest toggle (`news-ai-digest`)
  - Digest category multi-select chip group (`news-digest-categories`)
  - YouTube suggestions toggle (`news-youtube-enabled`)
  - Article cache hours input (`news-cache-hours`)
- Wired all controls in `public/js/app.js`:
  - `news-ai-digest` toggle saves to `settings.newsHub.aiDigestEnabled` via `saveSettings`
  - `news-default-category` saves to `settings.newsHub.defaultCategory`
  - `news-digest-categories` chip group updates `settings.newsHub.digestCategories` array
  - `news-youtube-enabled` toggle saves to `settings.newsHub.youtubeEnabled`
  - `news-cache-hours` input saves to `settings.newsHub.articleCacheHours`
- Added `.settings-chip-group` + `.settings-chip` styles in `public/css/base.css` with active state theming.
- Updated `WHITEBOARD.md` → `T-053-e` marked DONE.

## Goal
Add News Hub settings in the Settings view.

## Acceptance Criteria
- [x] New settings section "News Hub" with schema fields from T-053 spec.
- [x] Fields: default category, AI digest toggle, digest categories multi-select, YouTube enabled toggle, article cache hours.
- [x] Settings saved via existing settings save helper.
- [x] Frontend reads settings on boot and applies defaults.

## Files Touched
- `public/index.html`
- `public/js/app.js`
- `public/css/base.css`

## Dependencies
T-053-d for AI digest toggle field.
