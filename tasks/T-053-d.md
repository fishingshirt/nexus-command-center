# T-053-d: News Hub — AI Daily Digest

## Status
`PENDING`

## Goal
Toggle LLM-generated daily digest of top headlines.

## Acceptance Criteria
- [ ] Settings panel toggle: `Enable AI Daily Digest` (default OFF).
- [ ] Toggle stored in settings under `newsHub.aiDigestEnabled`.
- [ ] Agent wake cycle: if enabled and `lastDigest` > 4h stale, call `/api/news/digest`.
- [ ] `/api/news/digest` triggers LLM summary generation (placeholder for now).
- [ ] Digest stored server-side in `~/.hermes/nexus-news-digest.json`.
- [ ] Frontend digest banner at top of News Hub; dismissible per card.
- [ ] "Top story of the day" highlighted.

## Files Expected to Touch
- `public/js/apps/news.js`
- `public/js/settings.js`
- `scripts/nexus-server.py` (`/api/news/digest`, `/api/news/digest/:id`)

## Dependencies
T-042 Notifications center (for digest-ready toast).
