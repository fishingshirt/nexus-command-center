# T-061-a — Wishlist App Shell

> **Parent:** T-061 Wishlist App  
> **Priority:** MEDIUM  
> **Status:** IN_PROGRESS  
> **Created:** 2026-05-10

---

## Scope

This sub-task only wires the **app shell** into the dashboard. No data logic, no list rendering. Just the 7-file touch sequence so the app appears in the grid, nav, router, and has a styled view ready for T-061-b.

---

## 7-File Touch Sequence

### 1. `public/index.html` `<head>`
Add:
```html
<link rel="stylesheet" href="css/apps/wishlist.css">
```

### 2. `public/index.html` nav drawer
Add before the final `</div>` of `.nav-drawer-body`:
```html
<a href="#wishlist" class="nav-link" data-app="wishlist">
  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
    <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
  </svg>
  Wishlist
</a>
```

### 3. `public/index.html` dashboard app grid
Add a card inside `#dashboard-grid`:
```html
<div class="app-card" data-app="wishlist" tabindex="0" role="button" aria-label="Wishlist">
  <div class="app-card-icon">❤️</div>
  <div class="app-card-title">Wishlist</div>
  <div class="app-card-desc">Save items, track prices, and shop smarter.</div>
</div>
```

### 4. `public/index.html` views
Add after the last existing `.view` inside `<main class="app-main">`:
```html
<!-- Wishlist View -->
<div id="view-wishlist" class="view">
  <div class="view-header">
    <button class="view-back" data-view="dashboard" aria-label="Back to dashboard">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="19" y1="12" x2="5" y2="12"></line>
        <polyline points="12 19 5 12 12 5"></polyline>
      </svg>
    </button>
    <h2>Wishlist</h2>
    <button class="view-action" id="wishlist-add-btn" aria-label="Add item">+ Add</button>
  </div>
  <div class="view-body wishlist-view-body">
    <div class="wishlist-empty" id="wishlist-empty">
      <div class="wishlist-empty-icon">🛍️</div>
      <p>No items yet.</p>
      <span>Tap "+ Add" to save your first wishlist item.</span>
    </div>
    <div class="wishlist-grid" id="wishlist-grid"></div>
  </div>
</div>
```

### 5. `public/js/apps/wishlist.js`
```javascript
const LS_KEY = 'ncc-wishlist-v1';

function loadData() {
  try { return JSON.parse(localStorage.getItem(LS_KEY)) || {}; }
  catch { return {}; }
}
function saveData(patch) {
  const d = loadData();
  localStorage.setItem(LS_KEY, JSON.stringify({ ...d, ...patch }));
}
function ensureData() {
  const d = loadData();
  if (!d.items) d.items = [];
  saveData(d);
  return d;
}

export function initWishlist() {
  ensureData();
  renderWishlist();
  document.getElementById('wishlist-add-btn')?.addEventListener('click', () => {
    // T-061-b will wire the add modal
    showWishlistToast('Add item coming in T-061-b');
  });
}

function renderWishlist() {
  const grid = document.getElementById('wishlist-grid');
  const empty = document.getElementById('wishlist-empty');
  if (!grid || !empty) return;
  const d = loadData();
  if (!d.items || !d.items.length) {
    grid.style.display = 'none';
    empty.style.display = 'flex';
    return;
  }
  grid.style.display = 'grid';
  empty.style.display = 'none';
  // T-061-c will render cards here
}

function showWishlistToast(msg) {
  const container = document.getElementById('toast-container');
  if (!container) return;
  const el = document.createElement('div');
  el.className = 'toast';
  el.textContent = msg;
  container.appendChild(el);
  requestAnimationFrame(() => el.classList.add('show'));
  setTimeout(() => { el.classList.remove('show'); setTimeout(() => el.remove(), 300); }, 2500);
}
```

### 6. `public/js/app.js`
- Import: `import { initWishlist } from './apps/wishlist.js';`
- Registry: add `{ id: 'wishlist', name: 'Wishlist', icon: '❤️', path: 'wishlist' }` to `APP_REGISTRY`
- Init call: `initWishlist();` inside `initApp()`

### 7. `public/css/apps/wishlist.css`
```css
/* ===== WISHLIST APP ===== */
.wishlist-view-body {
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}

.wishlist-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--space-3);
  padding: var(--space-8) var(--space-4);
  color: var(--text-secondary);
  text-align: center;
}
.wishlist-empty-icon { font-size: 3rem; }
.wishlist-empty p { margin: 0; font-weight: 600; color: var(--text); }
.wishlist-empty span { font-size: 0.875rem; }

.wishlist-grid {
  display: none;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: var(--space-4);
}

/* Mobile */
@media (max-width: 480px) {
  .wishlist-grid { grid-template-columns: 1fr; }
}
```

---

## Acceptance Criteria
- [ ] App appears in dashboard grid with ❤️ icon
- [ ] App appears in nav drawer with heart SVG
- [ ] Clicking card or nav link routes to `#wishlist`
- [ ] View shows empty state with 🛍️ icon and "No items yet."
- [ ] Back button returns to dashboard
- [ ] `localStorage` key `ncc-wishlist-v1` initialized as `{ items: [] }`
- [ ] No console errors on load
