# T-029: IT Hub / System Health

## Status
`DONE`

## Goal
IT administrative dashboard inside the Backup app — monitors server health, system resources, network status, auth state, dependencies, and logs. Cards auto-refresh every 10 seconds.

## Sub-tasks
- `T-029-a` — IT Hub card grid (server, health, network, deps, logs, auth)
- `T-029-b` — Backend API endpoints (`/api/system/*`, `/api/server/status`)
- `T-029-c` — Status dot traffic-light + copy status report
- `T-029-d` — Settings toggle visibility on main dashboard

## Acceptance Criteria
- [x] Six health cards render inside `#view-backup`
- [x] Server status shows PID, port, uptime, branch, commit
- [x] System health shows CPU, RAM, disk, load averages
- [x] Network / Tailscale status with peers count
- [x] Auth card shows per-app PIN lock status
- [x] Service matrix with pass/warn/error indicators
- [x] Logs tail with error log preview
- [x] Auto-refreshes every 10 seconds
- [x] Settings → IT Hub visibility toggle

## Files Touched
- `public/js/apps/it-hub.js`
- `public/js/apps/backup.js`
- `public/css/apps/it-hub.css`
- `public/index.html` (nav link, settings section)
- `scripts/nexus-server.py` (`/api/system/*`, `/api/server/*`)

## Dependencies
None.
