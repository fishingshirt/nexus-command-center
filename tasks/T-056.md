# T-056: Fix Weather App Layout Centering

## Status
`PENDING`

## Goal
The Weather app view has layout alignment issues where content appears off to the side/bottom instead of being properly centered. Fix the search bar, current weather hero, details grid, and forecast section so everything is centered and balanced within the view.

## Acceptance Criteria
- [ ] Weather search row is centered horizontally within the view-body
- [ ] `.weather-hero` remains centered with proper max-width constraint
- [ ] `.weather-details` grid is centered with max-width and gap consistency
- [ ] `.weather-forecast` section is centered with max-width
- [ ] `.weather-forecast-grid` items are centered when fewer than 5 days are shown or on wider screens
- [ ] Add `max-width` to weather content containers so they don't stretch awkwardly on large screens
- [ ] Ensure weather app card on dashboard (`.app-card[data-app="weather"]`) has consistent padding/icon alignment
- [ ] Test on mobile (≤480px) and desktop to verify centering works across breakpoints

## UI Details
- Use `margin: 0 auto` + `max-width: 600px` for main weather content wrapper
- Forecast grid should use `justify-items: center` or `justify-content: center`
- Keep existing mobile styles; only adjust desktop/wide-screen centering

## Files Expected to Touch
- `public/css/apps/weather.css`
- `public/index.html` (minor: weather view-body wrapper markup if needed)

## Dependencies
- None
