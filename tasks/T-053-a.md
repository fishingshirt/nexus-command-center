# T-053-a: News Hub — App Shell & Registration

## Status
`PENDING`

## Goal
Create the News & Media Hub app shell: HTML slot, CSS framework, JS module registration, and basic server endpoint stubs.

## Acceptance Criteria
- [ ] Add `<section id="app-news">` slot in `public/index.html` after other app sections.
- [ ] Create `public/js/apps/news.js` with minimal shell: `init()`, `open()`, `close()`, app object registering in `APP_REGISTRY`.
- [ ] Create `public/css/apps/news.css` with base styles: header, filter chip bar, news list container, YouTube strip placeholder, digest banner placeholder. All colors via CSS vars.
- [ ] Register the app in `public/js/apps.js` (or wherever `APP_REGISTRY` lives) with id `news`, name `News Hub`, icon `📰`, category `media`.
- [ ] Add server endpoint stubs in `scripts/nexus-server.py`:
  - `GET /api/news` → returns `{"articles":[], "totalResults":0}`
  - `GET /api/news/categories` → returns category list
  - `GET /api/youtube/daily` → returns `{"videos":[]}`
  - `POST /api/news/digest` / `GET /api/news/digest/:id` → minimal stubs
- [ ] Update `WHITEBOARD.md` → mark T-053-a `DONE`, T-053-b `IN_PROGRESS` after this sub-task.

## Files to Touch
- `public/index.html`
- `public/js/apps/news.js`
- `public/css/apps/news.css`
- `public/js/apps.js`
- `scripts/nexus-server.py`

## Max Lines
~150
