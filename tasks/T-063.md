# T-063 — Calendar Service Email Integration

> **Status:** `BLOCKED` — awaiting user prerequisites  
> **Priority:** MEDIUM  
> **Depends on:** User providing API credentials + calendar permissions  

---

## 📋 Description

Integrate an external service (via email/API) into the Nexus Command Center Calendar app. This integration will require read/write access to the user's personal calendar to sync events bidirectionally or process calendar-related actions.

The user has indicated they will:
1. Provide an **API key/token** for a service email
2. Grant that email **read + write permissions** on their personal calendar
3. Return to this task later when prerequisites are ready

---

## 🚧 Prerequisites (User Action Required)

Before work can begin, the user must provide:

| # | Prerequisite | How to provide |
|---|-------------|----------------|
| 1 | **Service email API credentials** | Add to `~/.hermes/.env` (e.g. `CALENDAR_SERVICE_API_KEY`, `CALENDAR_SERVICE_EMAIL`) |
| 2 | **Calendar read/write permissions** granted to the service email | Share personal calendar (Google/Outlook/etc.) with the service email, granting "Make changes to events" permission |
| 3 | **Service name / API docs** (if non-obvious) | Tell the agent which service this is (e.g. Google Calendar API, Cal.com, Cronofy, etc.) |

---

## 🎯 Acceptance Criteria (to be refined when unblocked)

- [ ] Service can read events from the user's personal calendar
- [ ] Service can write/create events to the user's personal calendar
- [ ] Nexus Calendar UI reflects the integrated service (new events appear, status shown)
- [ ] Error handling for auth failures / rate limits / sync conflicts
- [ ] Settings panel toggle to enable/disable the integration
- [ ] Credentials stored securely in `~/.hermes/.env` (never committed)

---

## 🔒 Security Notes

- API keys must go in `~/.hermes/.env` (gitignored)
- Server-side proxy should hold the actual key; frontend never sees it raw
- Use the existing `settings`/`env` pattern from T-017-a (Spotify) and T-034 (Google Calendar)

---

## 📝 Agent Notes

- **Created:** 2026-05-14
- **Blocked reason:** User not at computer, credentials not yet available
- **Next step:** Wait for user to provide (1) service name/docs, (2) API credentials, (3) confirm calendar permissions granted
- **When unblocked:** Read this file, confirm prerequisites met, then spec the integration and build.
