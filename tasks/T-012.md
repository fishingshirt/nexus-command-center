# T-012: Hermes API Bridge

## Status
`PENDING`

## Goal
Wire the dashboard chat widget to the real Hermes agent so messages flow both ways.

## Acceptance Criteria
- [ ] Dashboard chat widget sends messages to Hermes
- [ ] Hermes responses appear in the chat widget
- [ ] `/new` command clears local history and starts fresh context
- [ ] Works when dashboard is served from localhost

## Research Notes
Options:
1. Telegram Bot API — forward messages to a bot we own
2. Hermes local webhook (not yet available)
3. Discord DM bridge via existing Discord connection

**Preferred:** Option 1 (Telegram) because it already runs here.

## Files Expected to Touch
- `public/js/chat.js`
- `nexus-server.py` (new endpoint `/api/hermes/message`)

## Dependencies
None (T-014 auth already done).
