# T-020-b: AI Task Suggester — CSS Module

## Status
`PENDING`

## Goal
Create the AI Suggester stylesheet.

## Acceptance Criteria
1. Create `public/css/apps/ai-suggester.css`:
   - `.suggester-view` container with grid layout for cards
   - `.suggestion-card` with left colored priority stripe (`HIGH`=red, `MEDIUM`=amber, `LOW`=green)
   - `.suggestion-card__title`, `.suggestion-card__desc`, `.suggestion-card__meta` (category tag)
   - `.suggestion-card__actions` row with two buttons: primary "Add to To-Do", secondary "Dismiss"
   - Responsive: 1 column at <768px, 2 columns above
   - Loading skeleton animation using `shimmer` keyframe
   - Respect `prefers-reduced-motion`

## Files Expected to Touch
- `public/css/apps/ai-suggester.css` (new)

## Dependencies
T-020-a
