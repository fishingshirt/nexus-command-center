# T-032: Email Agent / Smart Inbox

## Status
`IN_PROGRESS` — T-032-a DONE, T-032-b DONE, T-032-c DONE, T-032-d DONE, T-032-e DONE, T-032-f PARTIAL, T-032-g DONE

## Goal
Gmail-integrated smart inbox with AI-powered priority triage and auto-drafting replies.

## Sub-tasks
- `T-032-a` — Gmail OAuth2 backend (read/write scope, token store) — **DONE** ✅
- `T-032-b` — Inbox UI shell (folders, list, thread, compose) — **DONE** ✅
- `T-032-c` — Read/fetch engine (Gmail API thread list/read) — **DONE** ✅
- `T-032-d` — Compose & send (Gmail API send, draft) — **DONE** ✅
- `T-032-e` — AI Email Agent toggle + cron job — **DONE** ✅
- `T-032-f` — Smart inbox UI (priority badges, draft preview) — **DONE** ✅
- `T-032-g` — Welcome screen overlay — **DONE** ✅

## Acceptance Criteria
- [x] Gmail OAuth connects and reads threads
- [x] Categorizes emails (Urgent, Action Required, FYI, Spam)
- [x] Works in both desktop and mobile views
- [x] Drafts replies that user approves before sending (AI backend integrated, Ollama required)

## Architecture
- **Backend:** `scripts/nexus-server.py` adds `/api/email/oauth/start`, `/api/email/oauth/callback`, `/api/email/status`, `/api/email/unlink`, `/api/email/threads`, `/api/email/send`, `/api/email/draft`, `/api/email/ai-draft`
- **Cron agent:** `scripts/nexus-email-agent.py` — scans urgent/action threads, drafts via Ollama, saves Gmail drafts
- **Frontend:** `public/js/apps/email.js` registers in app registry; `public/css/apps/email.css` for inbox layout
- **Storage prefix:** `ncc-email-*` for local cache; `~/.hermes/nexus-gmail-tokens.json` for OAuth tokens
- **Dependencies:** Reuses calendar OAuth credential loading from `~/.hermes/.env`; requires `ollama` running locally for AI draft
