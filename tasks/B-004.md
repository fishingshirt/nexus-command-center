# B-004: Broken HTML structure after T-032-b merge

## Severity
HIGH

## Status
FIXED

## Description
The T-032-b commit introduced malformed HTML in `public/index.html`, causing:
1. Wishlist nav link missing opening closure, resulting in nested `<a>` tags.
2. Email view (`view-email`) incorrectly placed inside `<main>` but outside dashboard grid, breaking DOM nesting.
3. The `</main>` tag misplaced — there was one extra unclosed `<div>`, creating invalid HTML.

## Fix
- Restored proper nav/wishlist link ordering.
- Fixed `view-email` so it closes properly within `<main>`.
- Added missing closing `</div>` after dashboard grid.
- Verified with Python HTMLParser depth tracker (final depth = 0, no mismatched tags).

## Files Modified
- `public/index.html` (multiple patches)
- `scripts/nexus-server.py` (added missing blank line + `/api/email/draft` endpoint)
- `tasks/T-032.md` (updated sub-task status)
