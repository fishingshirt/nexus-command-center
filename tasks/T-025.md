# T-025: Smart PDF Editor

## Status
`PENDING`

## Goal
Upload a PDF and tell the agent what to change — it loads PDF skills (`nano-pdf`, `pymupdf`) to edit.

## Acceptance Criteria
- [ ] Drag-and-drop PDF upload with thumbnail preview
- [ ] Natural language instruction parser: "change title to X", "merge with doc2.pdf", "extract pages 3-5", "OCR and replace text"
- [ ] Backend executes edits via Python `pymupdf` / `nano-pdf` CLI
- [ ] Version history: every edit creates a new version, user can rollback
- [ ] Preview edited PDF inline (`<iframe>` or `<canvas>`)
- [ ] Download final PDF

## Files Expected to Touch
- `public/js/apps/pdf-editor.js`
- `public/css/apps/pdf-editor.css`
- `nexus-server.py` — new `/api/pdf/*` endpoints
- `data/pdfs/` — PDF storage directory

## Agent Note
Break into `T-025-a` through `T-025-d`. Install `pymupdf` via `uv pip install pymupdf` on the host if missing.
